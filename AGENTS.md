# Repository working rules

이 저장소를 수정하는 사람과 자동화 도구는 다음 공개 저장소 기준을 항상 지켜야 합니다.

## 공개 안전성

- 소스, 문서, 이미지, 커밋 메시지에 특정 AI 공급자·모델·도구의 제품명이나 생성 도구 흔적을 넣지 않습니다. 설명은 공급자 중립적인 일반 용어로 작성합니다.
- 개인 워크스테이션의 절대 경로, 로컬 계정명, 홈 디렉터리, 호스트명, IDE 세션 정보와 도구 전용 메타데이터를 커밋하지 않습니다.
- 비밀번호, 토큰, 키, 인증서, `.env`, DB 덤프, 운영 로그, 빌드 산출물과 로컬 캐시는 커밋하지 않습니다.
- 화면 이미지는 보이는 계정·경로·알림뿐 아니라 파일 메타데이터와 포함 문자열도 확인합니다.
- 환경별 값은 코드에 고정하지 않고 환경변수, 설정 파일 또는 데이터베이스 설정으로 주입합니다.
- UI에 왼쪽 accent bar를 디자인 요소로 사용하지 않습니다.

## 커밋과 푸시 전 필수 확인

1. `./scripts/public-release-check.sh staged`로 스테이징 내용을 검사합니다.
2. 변경 범위에 맞는 테스트를 실행하고, 의존성을 바꿨다면 `./mvnw clean package`로 라이선스 보고서와 SBOM 생성까지 확인합니다.
3. README, 운영 문서, 보안 체크리스트와 제3자 고지가 실제 동작과 일치하는지 확인합니다.
4. 푸시 전 `./scripts/public-release-check.sh repository`로 추적 파일과 커밋 메시지를 다시 검사합니다.

저장소는 `.githooks`를 사용합니다. 최초 복제 후 `git config core.hooksPath .githooks`를 실행하세요. 개인적으로 차단할 문자열은 `.git/info/public-release-deny-patterns`에 한 줄에 하나씩 보관하며, 이 파일은 절대 저장소에 복사하지 않습니다. 검사 실패를 `--no-verify`로 우회하지 말고 원인을 제거합니다.

의도적인 예외가 필요하면 먼저 저장소 소유자의 확인을 받고, 공개 범위와 대체 표현을 문서화한 뒤 진행합니다.
